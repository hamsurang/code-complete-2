---
sidebar_position: 5
title: "Chapter 5. 구현 과정에서의 설계"
description: "복잡도 관리라는 대전제는 2026년 FE에서도 여전히 유효하다 — 결정이 필요한 지점만 프레임워크 경계 안쪽으로 좁아졌을 뿐."
authors:
  - Alice
  - Leo
  - zinii
ai_assisted:
  - summary.lead
  - summary.bullets
  - verdict.rationale
source: notion-pull
source_page_id: 561de71a-aa9f-8296-b752-8162200acdb6
week: 6주차
extracted_at: 2026-04-19T00:13:04+09:00
edited_at: 2026-04-19T00:30:00+09:00
enhanced_at: 2026-04-19T00:45:00+09:00
---

import Verdict from '@site/src/components/Verdict';
import MemberOpinion from '@site/src/components/MemberOpinion';
import DevilsAdvocate from '@site/src/components/DevilsAdvocate';

> 원제: _Design in Construction_

<Verdict
  rating="🟢 생존"
  rationale={`"복잡도 관리가 소프트웨어의 최우선 기술적 과제다"는 대전제는 컴포넌트 트리·상태 관리·빌드 설정이 얽힌 2026년 FE에서 오히려 더 절실해졌어요 — 도구만 바뀌었을 뿐 문제의 본질은 그대로예요.`}
/>

대전제가 왜 살아남았는지, 본문에서 하나씩 짚어볼게요.

## 📝 핵심 요약

구현 단계에서의 설계는 "한 번 그리고 끝"이 아니라 코드를 쓰면서 계속 다시 나누는 활동이에요. McConnell은 복잡도 관리를 최우선 과제로 놓고, 정보 은닉·느슨한 결합·높은 응집도처럼 여러 휴리스틱을 반복적으로 적용하라고 말해요. 2026년 FE도 그대로 유효한데, 결정 지점이 프레임워크 경계 안쪽 — 컴포넌트 분리 기준, 훅 경계, API 레이어 추상화 — 으로 좁아졌을 뿐이에요.

- 복잡도 관리가 설계의 목적이에요. 한 번에 읽어야 할 맥락을 줄이는 쪽으로 구조를 계속 다시 나눠요.
- 정보 은닉·느슨한 결합·높은 응집도는 독립된 규칙이 아니라 같은 문제(변경 전파)를 여러 각도에서 본 휴리스틱이에요.
- 설계는 반복·비결정적이에요. 한 번에 정답을 내지 않고 2~3개 안을 비교해서 더 나은 걸 고르는 게 정상이에요.
- 2026년 FE에서는 프레임워크가 라우팅·렌더링·파일 구조를 먹었지만, 상태 경계·훅 분리·API 레이어는 여전히 개발자 손에 남아요.

## 💻 React/TS 코드 예제

원칙은 문장으로만 보면 추상적이에요. 결제 내역 화면 하나로 같은 원칙이 어떻게 코드에 찍히는지 봐요.

### 심화 미션 — 설계가 아쉬운 코드 분석 + 개선 (PaymentsPage)

#### Before ❌

```tsx
function PaymentsPage() {
  const [data, setData] = useState([]);

  useEffect(() => {
    fetch("/api/payments")
      .then((r) => r.json())
      .then((list) => {
        const filtered = list.filter((p) => p.status === "paid");
        const sorted = filtered.sort((a, b) => b.createdAt - a.createdAt);
        setData(sorted);
      });
  }, []);

  return (
    <div>
      {data.map((p) => (
        <div key={p.id}>{p.amount}</div>
      ))}
    </div>
  );
}
```

**아쉬운 점**

- 관심사 혼재: 하나의 useEffect 안에 API 호출, 필터링, 정렬이 전부 섞여 있어요. "어디서 데이터를 가져오는가"와 "어떤 조건으로 가공하는가"가 구분되지 않아요.
- 타입 부재: `useState([])`는 `any[]`이며, `.status`, `.createdAt`, `.id`, `.amount` 중 어느 하나라도 오타가 나면 런타임에서야 드러나요.
- 테스트 불가: fetch가 컴포넌트 안에 하드코딩되어 있어서, 필터/정렬 로직만 단독으로 테스트할 수 없어요.
- 변경 취약: "환불 내역도 보여달라"는 요구사항이 오면 useEffect 콜백 내부를 직접 수정해야 해요.

#### After ✅

```tsx
// ━━ Types ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

type PaymentStatus = "paid" | "refunded" | "failed";

interface Payment {
  id: string;
  amount: number;
  status: PaymentStatus;
  createdAt: number;
}

// ━━ API 레이어: 데이터 패칭만 담당 ━━━━━━━━━

async function fetchPayments(): Promise<Payment[]> {
  const res = await fetch("/api/payments");
  return res.json();
}

// ━━ 도메인 로직: 필터·정렬은 순수 함수 ━━━━

function filterByStatus(payments: Payment[], status: PaymentStatus): Payment[] {
  return payments.filter((p) => p.status === status);
}

function sortByLatest(payments: Payment[]): Payment[] {
  return [...payments].sort((a, b) => b.createdAt - a.createdAt);
}

// ━━ 데이터 훅: 패칭 + 가공 조합 ━━━━━━━━━━━

function usePaidPayments() {
  const [payments, setPayments] = useState<Payment[]>([]);
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    setLoading(true);
    fetchPayments()
      .then((list) => {
        const paid = filterByStatus(list, "paid");
        const sorted = sortByLatest(paid);
        setPayments(sorted);
      })
      .finally(() => setLoading(false));
  }, []);

  return { payments, loading };
}

// ━━ UI: 데이터를 받아서 그리기만 ━━━━━━━━━━━

function PaymentsPage() {
  const { payments, loading } = usePaidPayments();

  if (loading) return <div>Loading...</div>;

  return (
    <div>
      {payments.map((payment) => (
        <div key={payment.id}>{payment.amount.toLocaleString()}원</div>
      ))}
    </div>
  );
}
```

**개선 포인트**

- API(`fetchPayments`), 도메인 로직(`filterByStatus`, `sortByLatest`), 조합(`usePaidPayments`), UI(`PaymentsPage`)가 각각 독립적으로 동작해요.
- `filterByStatus`와 `sortByLatest`는 순수 함수라 mock 없이 바로 단위 테스트할 수 있어요.
- "환불 내역도 보여달라" → `filterByStatus(list, "refunded")`를 추가하거나, 필터 조건을 파라미터로 바꾸면 돼요. 기존 함수는 건드리지 않아도 돼요.

## 💬 토론 주제

코드는 한 장면일 뿐이에요. 현업에서는 언제 이 결정을 다시 내릴지가 더 자주 부딪히는 질문이에요.

1. 구현 과정에서 설계를 바꾸게 되는 대표적인 상황은? 다시 나눌 때의 기준은 "감"이 아니라 무엇이어야 하나?
2. 결제 내역 목록 화면(조회/검색/필터/정렬/페이지네이션)을 어떤 단위로 코드를 나눌 것인가? 각자 선호하는 분리 방식은?

## 🗣️ 멤버 의견

이론이 아니라 실무에서 이 장면을 마주한 사람이 뭐라고 답했는지 들어봐요. 이번 주는 3명만 기록을 남겼어요.

<MemberOpinion
  author="Alice"
  emoji="🐻"
  opinion={`초기에 특정 기능에 맞춰 구현했지만 기획이 바뀌면서 유사한 기능이 여러 곳에서 재사용되어야 할 때 공통 컴포넌트/공통 로직으로 분리하는 방향으로 설계 수정. 기준은 UI 역할 / 비즈니스 로직 / 데이터 관리로 책임을 명확히 나누는 것. 공통 컴포넌트나 유틸을 만들 때는 도메인 지식이 들어가지 않도록 설계함.`}
  experience={`검색/필터/정렬/페이지네이션은 공통 목록 기능이라 특정 도메인에 결합되지 않도록 UI와 기능을 분리. 이 네 가지는 서로 영향을 주는 상태이기 때문에 하나의 상태 관리 훅으로 묶음. 도메인 지식이 포함된 로직은 상위 레벨에서 관리.`}
/>

<MemberOpinion
  author="Leo"
  emoji="🦁"
  opinion={`세 가지 상황에서 다시 나눔. ① 한 번에 인지해야 하는 정보의 양이 많아졌다고 느낄 때 ② 요구사항 변경에 따라 관심사가 많아졌을 때 ③ 중복 코드가 많아졌을 때. 단, DRY를 지킬 때 가장 중요한 것은 섣부른 추상화를 피하는 것 — 거짓된 중복, 우발적 중복에 속지 않기. 구조를 다시 나눌 땐 역할과 책임 기준으로.`}
  experience={`네 단위로 나눔. ① UI를 그리는 코드 ② UI 관련 로직(상태 필터, 정렬, 페이지네이션) ③ 유저 행위(검색) ④ 서버 API 호출(결제 목록 조회, 검색).`}
/>

<MemberOpinion
  author="zinii"
  emoji="🐣"
  opinion={`기획이 바뀌거나 UI가 크게 변경될 때. 데이터 기준으로 구조를 다시 나눔 — 주는 데이터와 받는 데이터에 따라 화면에 보여야 할/숨겨야 할 요소가 달라지기 때문.`}
  experience={`어렵게 느껴짐 — 연관 항목들이 서로 엮여있으니까. 데이터 단위로 분리하는 게 맞을 것 같음.`}
/>

## 😈 Devil's Advocate

<DevilsAdvocate
  author="unknown"
  argument={`설계 자유도의 상당 부분은 이미 프레임워크가 먹었다.
McConnell은 "여러 대안을 시도하고, 반복하고, 기록하라"고 한다. 하지만 2026년 FE에서 Next.js App Router, Remix 같은 프레임워크는 라우팅·데이터 페칭·렌더링 전략·파일 구조를 상당 부분 강제한다. 개발자가 실제로 설계 결정을 내리는 영역은 생각보다 좁다 — 상태 관리 경계, API 레이어 추상화, 컴포넌트 분리 기준 정도. 이 좁은 영역에 집중하는 게 "모든 곳에서 설계를 반복하라"보다 현실적이라는 관점이 있다. 반대로, 프레임워크가 정해준 레일 위에서도 "왜 이 구조인가"를 이해하지 못하면 프레임워크가 예상하지 못한 상황에서 무너진다는 관점도 있다. 설계가 필요 없어진 게 아니라, 설계가 필요한 지점이 달라진 것이다.`}
/>

## ✅ FE 실전 체크리스트

마지막은 코드 리뷰에서 바로 꺼내 쓸 수 있는 다섯 가지 질문이에요.

- [ ] "이 훅/컴포넌트가 숨기는 비밀이 무엇인가?"라는 질문에 한 문장으로 답할 수 있는가? (정보 은닉)
- [ ] API 응답 구조, 외부 라이브러리 인터페이스 등 변경 가능성이 높은 결정이 한 곳에 격리되어 있는가?
- [ ] 컴포넌트 간 순환 import가 없는가? — 관련 ESLint: [`import/no-cycle`](https://github.com/import-js/eslint-plugin-import/blob/main/docs/rules/no-cycle.md)
- [ ] 컴포넌트/훅 구조를 최소 2가지 이상 시도하고, 첫 번째가 아닌 더 나은 안을 선택했는가?
- [ ] 설계가 lean한가? 모든 추상화 레이어에 존재 이유가 있는가, 아니면 "나중에 필요할 것 같아서" 만든 레이어가 있는가?

:::note 이번 장에서 생략된 섹션
- **투표 (VotingBar):** 스터디원 투표가 이번 주 생략됐어요 (전원 미응답).
- **베스트 픽 (BestPickCallout):** 베스트 토론이 선정되지 않았어요.
:::
